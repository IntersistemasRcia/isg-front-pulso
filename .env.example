# Plantilla de variables. Next.js carga automÃ¡ticamente:
#   - `.env.development` con `npm run dev`  (API Auth de test)
#   - `.env.production`  con `npm run build` / `npm start` (API local same-server)
#   - `.env.local`       overrides locales (gitignored; secretos)

# API Auth
# Test:    http://api.intersistemas.ar:8601
# Prod:    http://localhost:8601  (mismo servidor que el front)
AUTH_API_URL=http://api.intersistemas.ar:8601

# Endpoint usado por el proxy: POST {AUTH_API_URL}/api/Auth/Login
# Body: { "usuario": "...", "password": "..." }
# Response: { "token": "...", "validTo": "..." }

# Secreto HS512 de API Auth (recomendado en prod; en test puede omitirse)
JWT_SECRET=

# Fallback local SOLO si AUTH_API_URL estÃ¡ vacÃ­o
LOCAL_AUTH_USER=demo
LOCAL_AUTH_PASSWORD=demo

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Multi-LLM — tier free
# Obtener en: https://aistudio.google.com/apikey
# Formatos válidos: AIzaSy… (standard) o AQ.Ab… (auth key 2026+)
GOOGLE_FREE_API_KEY=
# Opcional: backup free vía Groq — https://console.groq.com/keys
GROQ_API_KEY=

# Proveedor local OpenAI-compatible (Ollama, LM Studio, vLLM…)
# El front no cambia: aparece en el selector si LOCAL_LLM_ENABLED=1
LOCAL_LLM_ENABLED=
LOCAL_LLM_BASE_URL=http://127.0.0.1:11434/v1
LOCAL_LLM_API_KEY=ollama
LOCAL_LLM_MODEL=llama3.1
LOCAL_LLM_LABEL=LLM Local
# Presupuesto de tokens (default 8192). Más bajo = prompts más chicos y más rápido.
LOCAL_LLM_MAX_INPUT_TOKENS=8192
# Pasos máximos del agente (default 2: 1 tool + respuesta). Menos = más rápido.
LOCAL_LLM_MAX_AGENT_STEPS=2

# Premium por empresa (alternativa a BYOK por usuario)
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# ==========================================
# PACK PULSO IA PREMIUM (Hosted / Empresarial)
# ==========================================
# Activar el pack hosted (gemini-2.5-flash como modelo principal de empresa)
HOSTED_LLM_ENABLED=

# ID del proyecto GCP de este cliente en tu Billing Account empresa.
# Usado para mostrar mensajes de error más precisos y diagnóstico.
HOSTED_GCP_PROJECT_ID=

# Cupos del pack (soft limits; Pulso avisa antes de que GCP corte).
# Consultas de usuario por día (un "chat" puede ser 2-5 requests a la API).
HOSTED_DAILY_CHATS=
# Tokens estimados por mes (input+output, estimación Pulso).
HOSTED_MONTHLY_TOKENS=
# Cuándo avisar "te estás acercando al límite" (0.0–1.0, default 0.8 = 80%)
HOSTED_NEAR_DAILY_RATIO=0.8
HOSTED_NEAR_MONTHLY_RATIO=0.8

# Tokens por minuto máx. (soft limit local, default 50000).
# Configurá el mismo valor en GCP → IAM → Quotas del proyecto.
HOSTED_MAX_TOKENS_PER_MIN=50000

# Cifrado AES-256-GCM de claves BYOK en data/byok/
BYOK_ENCRYPTION_KEY=


# isg-api-pulso (.NET 10)
NEXT_PUBLIC_PULSO_API_URL=https://localhost:44351/api/v1/pulso
# PULSO_API_URL=https://localhost:44351/api/v1/pulso
# PULSO_API_TOKEN=   # opcional; por defecto se usa el JWT de la sesiÃ³n
# PULSO_TLS_INSECURE=1  # solo local con certificado autofirmado

LOCAL_AGENT_URL=http://localhost:5100
LOCAL_AGENT_API_KEY=change-me-agent-key

NEXT_PUBLIC_API_URL=
