# Entorno de desarrollo / test (cargado automáticamente por `next dev`)
# API Auth de prueba remota
AUTH_API_URL=http://api.intersistemas.ar:8601

# Sin JWT_SECRET: se valida estructura + expiración del token de Auth.
# Cuando tengas el secreto de firma (HS512), descomentá:
# JWT_SECRET=

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

LOCAL_AGENT_URL=http://localhost:5100
LOCAL_AGENT_API_KEY=change-me-agent-key

NEXT_PUBLIC_API_URL=
